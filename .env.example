# Required: choose one model access mode
# Supported providers: openai, anthropic, gemini, openai_compatible, claude_subscription, codex_subscription

# ------------------------
# Subscription mode (recommended, no API key in project env)
# ------------------------
# One-time local login (choose one):
# codex login
# claude   # then run /login in the interactive session

# Option A: Codex subscription
# MODEL_PROVIDER=codex_subscription
# MODEL_NAME=gpt-5.3-codex
# CODEX_SUBSCRIPTION_BIN=codex

# Option B: Claude subscription
# MODEL_PROVIDER=claude_subscription
# MODEL_NAME=sonnet
# CLAUDE_SUBSCRIPTION_BIN=claude

# ------------------------
# API key mode examples
# ------------------------
# OpenAI:
# MODEL_PROVIDER=openai
# MODEL_NAME=gpt-4.1-mini
# MODEL_API_KEY=<openai_key>
#
# Anthropic:
# MODEL_PROVIDER=anthropic
# MODEL_NAME=claude-3-5-sonnet-latest
# MODEL_API_KEY=<anthropic_key>
#
# Gemini:
# MODEL_PROVIDER=gemini
# MODEL_NAME=gemini-2.0-flash
# MODEL_API_KEY=<gemini_key>
#
# OpenAI-compatible gateway:
# MODEL_PROVIDER=openai_compatible
# MODEL_NAME=<provider_model_name>
# MODEL_API_KEY=<gateway_key>
# MODEL_BASE_URL=<gateway_base_url>

# Default template values (demo-first: one-time local login)
MODEL_PROVIDER=codex_subscription
MODEL_NAME=gpt-5.3-codex
# MODEL_API_KEY=your_model_api_key

# Optional: reasoning effort (minimal, low, medium, high, xhigh)
# MODEL_REASONING_EFFORT=xhigh

# Optional: custom model endpoint / headers
# MODEL_BASE_URL=
# MODEL_ORGANIZATION=

# Optional provider-specific fallback keys (used only when MODEL_API_KEY is not set)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Optional subscription CLI binaries
# CODEX_SUBSCRIPTION_BIN=codex
# CLAUDE_SUBSCRIPTION_BIN=claude

# Optional runtime overrides
TIDB_ZERO_TAG=agent-demo
TIDB_DB_NAME=agent_sandbox
RUNS_DIR=.runs
MAX_TOOL_ITERATIONS=24
SQL_ROW_LIMIT=200
HTTP_TIMEOUT_SEC=20
MODEL_TIMEOUT_SEC=120
MODEL_MAX_RETRIES=3
FETCH_MAX_CHARS=60000

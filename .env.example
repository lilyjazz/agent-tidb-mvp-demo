# Required: model provider + key
# Supported providers: openai, anthropic, gemini, openai_compatible
MODEL_PROVIDER=openai
MODEL_NAME=gpt-4o-mini
MODEL_API_KEY=your_model_api_key

# Optional: custom model endpoint / headers
# MODEL_BASE_URL=
# MODEL_ORGANIZATION=

# Optional provider-specific fallback keys (used only when MODEL_API_KEY is not set)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Required during TiDB Zero technical preview
TIDB_ZERO_INVITATION_CODE=your_invitation_code

# Optional runtime overrides
TIDB_ZERO_TAG=agent-demo
TIDB_DB_NAME=agent_sandbox
RUNS_DIR=.runs
MAX_TOOL_ITERATIONS=24
SQL_ROW_LIMIT=200
HTTP_TIMEOUT_SEC=20
FETCH_MAX_CHARS=60000

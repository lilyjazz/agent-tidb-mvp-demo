# Option A: Subscription Mode (Recommended, No API Key)
# Uncomment to use local CLI subscription (requires 'codex' or 'claude' CLI installed)
# MODEL_PROVIDER=codex_subscription
# MODEL_NAME=default

# Option B: API Key Mode
# Providers: openai (default), anthropic, gemini, openai_compatible
MODEL_PROVIDER=openai
MODEL_NAME=gpt-4o-mini
MODEL_API_KEY=

# [Optional] Runtime Settings
# TIDB_ZERO_TAG=agent-demo
# MAX_TOOL_ITERATIONS=24
# MODEL_TIMEOUT_SEC=120
